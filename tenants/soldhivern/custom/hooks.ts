import type { ExtensionRegistry } from '@logic-camp/core';

/** Sol d'Hivern expresa la larga estancia mediante datos, no mediante forks del core. */
export function register(ext: ExtensionRegistry): void {
  void ext;
}
