{
  "name": "@logic-camp/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy:demo": "wrangler deploy --config ../../tenants/demo/wrangler.jsonc"
  },
  "dependencies": {
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@logic-camp/tsconfig": "workspace:*",
    "vitest": "^3.2.0",
    "wrangler": "^4.24.0"
  }
}
