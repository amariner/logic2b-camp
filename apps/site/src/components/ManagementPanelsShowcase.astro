---
import ManagementPanelExplorer from './ManagementPanelExplorer.astro';
import type { Locale } from '../lib/i18n';
interface Props { locale: Locale; content: any; href: string }
const { locale, content, href } = Astro.props;
---
<ManagementPanelExplorer locale={locale} content={content} href={href} mode="home" />
