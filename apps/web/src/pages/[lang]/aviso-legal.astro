---
import Legal from '../../components/paginas/Legal.astro';
import { extraLocales } from '../../lib/content';

export function getStaticPaths() {
  return extraLocales.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---

<Legal locale={lang!} slug="aviso-legal" />
