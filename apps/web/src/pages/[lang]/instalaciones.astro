---
import Instalaciones from '../../components/paginas/Instalaciones.astro';
import { extraLocales } from '../../lib/content';

export function getStaticPaths() {
  return extraLocales.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---

<Instalaciones locale={lang!} />
