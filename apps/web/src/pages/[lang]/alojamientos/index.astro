---
import Alojamientos from '../../../components/paginas/Alojamientos.astro';
import { extraLocales } from '../../../lib/content';

export function getStaticPaths() {
  return extraLocales.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---

<Alojamientos locale={lang!} />
